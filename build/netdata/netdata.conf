[global]
    run as user = root

[db]
    mode = ram

[ml]
    enabled = no

[health]
    enabled = no

[web]
    mode = none

[registry]
    enabled = no

[telemetry]
    enabled = no

[cloud]
    enabled = no