// Package remoteshell owns remote shell execution decisions and transport.
//
// Remote shell egress is derived from the AppOS-side tunnel endpoint so the
// managed server never receives raw external proxy coordinates.
package remoteshell
