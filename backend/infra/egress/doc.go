// Package egress provides AppOS shared outbound network policy runtime.
//
// It owns the registry of known outbound surfaces plus the shared settings,
// capability, HTTP transport, and proxy-environment resolution used by
// platform consumers.
package egress
