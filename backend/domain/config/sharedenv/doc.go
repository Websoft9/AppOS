// Package sharedenv provides normalized shared environment read models and
// lookup services for config-domain consumers.
//
// The package intentionally hides PocketBase record details from callers and
// exposes only normalized shared-env objects and lookup operations.
package sharedenv
