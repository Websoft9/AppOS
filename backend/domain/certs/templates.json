[
  {
    "id": "self_signed_basic",
    "label": "Self-Signed Certificate",
    "kind": "self_signed",
    "description": "Create a certificate object and let the server generate both the certificate chain and a referenced TLS private key secret.",
    "fields": [
      { "key": "name", "label": "Name", "type": "text", "required": true },
      { "key": "domain", "label": "Domain / CN", "type": "text", "required": true },
      { "key": "description", "label": "Description", "type": "textarea" }
    ]
  },
  {
    "id": "ca_import_basic",
    "label": "CA-Issued Certificate (Import)",
    "kind": "ca_issued",
    "description": "Create a certificate object by importing the certificate chain and referencing an existing TLS private key secret.",
    "fields": [
      { "key": "name", "label": "Name", "type": "text", "required": true },
      { "key": "domain", "label": "Domain / CN", "type": "text", "required": true },
      { "key": "cert_pem", "label": "Certificate Chain (PEM)", "type": "textarea", "required": true, "upload": true },
      { "key": "private_key_secret", "label": "TLS Private Key Secret", "type": "relation", "required": false },
      { "key": "description", "label": "Description", "type": "textarea" }
    ]
  }
]
