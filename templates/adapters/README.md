## Template Adapters

This folder records how AppOS adapts upstream sources into normalized templates.

Purpose:

- keep adaptation intent explicit
- document automatic normalization targets versus review-required decisions
- support later automation without hiding product meaning in ad hoc code only

This folder is not the active template contract, not the upstream source store, and not the control-plane instance declaration store.
