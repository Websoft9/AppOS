{
  "templateKey": "mysql",
  "upstreamRef": "templates/upstream/mysql/source.json",
  "normalizedTemplateRef": "templates/apps/mysql",
  "adapterVersion": "v0",
  "automaticNormalization": [
    "service inventory",
    "image and tag extraction",
    "env and env-file references",
    "port exposure extraction",
    "volume and network extraction",
    "basic service-role candidate mapping"
  ],
  "reviewRequired": [
    "primary service identity",
    "secret-backed value classification",
    "publish semantics",
    "backup-critical data paths"
  ],
  "currentDecisions": {
    "primaryService": "mysql",
    "databaseService": null,
    "publishKind": "tcp",
    "secretBackedInputs": [
      "db_password"
    ]
  },
  "notes": [
    "This is sample adapter metadata for Epic 32 story work.",
    "MySQL is a single-service application with a built-in datastore, so there is no separate database service.",
    "db_password maps to MYSQL_ROOT_PASSWORD, the initial root password on first start.",
    "It describes adaptation intent before a full adapter engine exists."
  ]
}
