## Metabase Upstream Reference

This folder records the upstream source metadata for the Metabase template sample.

Primary normalized template:

- `templates/apps/metabase/`

Primary adapter metadata:

- `templates/adapters/metabase.json`
