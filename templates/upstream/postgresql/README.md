## PostgreSQL Upstream Reference

This folder records the upstream source metadata for the PostgreSQL template sample.

Primary normalized template:

- `templates/apps/postgresql/`

Primary adapter metadata:

- `templates/adapters/postgresql.json`
