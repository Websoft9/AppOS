## WordPress Upstream Reference

This folder records the upstream source metadata for the WordPress template sample.

It is intentionally light in the first pass. The goal is traceability, not a full source mirror.

Primary normalized template:

- `templates/apps/wordpress/`

Primary adapter metadata:

- `templates/adapters/wordpress.json`
