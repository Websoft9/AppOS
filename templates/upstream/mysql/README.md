## MySQL Upstream Reference

This folder records the upstream source metadata for the MySQL template sample.

Primary normalized template:

- `templates/apps/mysql/`

Primary adapter metadata:

- `templates/adapters/mysql.json`
