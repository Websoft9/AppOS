## phpMyAdmin Upstream Reference

This folder records the upstream source metadata for the phpMyAdmin template sample.

Primary normalized template:

- `templates/apps/phpmyadmin/`

Primary adapter metadata:

- `templates/adapters/phpmyadmin.json`
