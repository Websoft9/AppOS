## MariaDB Upstream Reference

This folder records the upstream source metadata for the MariaDB template sample.

Primary normalized template:

- `templates/apps/mariadb/`

Primary adapter metadata:

- `templates/adapters/mariadb.json`
