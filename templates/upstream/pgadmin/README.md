## pgAdmin Upstream Reference

This folder records the upstream source metadata for the pgAdmin template sample.

Primary normalized template:

- `templates/apps/pgadmin/`

Primary adapter metadata:

- `templates/adapters/pgadmin.json`
